# 面试问答库（已迁移）

**常见面试题及回答**已统一放在仓库根目录的 **`interview_qa/`** 文件夹中。

请直接使用：

- **目录**：`../interview_qa/`（即仓库根下的 `interview_qa/`）
- **查看与搜索**：`python generate.py interview --category technical` 或 `--role android`、`--search "关键词"`

本目录（`kb/interview_qa/`）保留仅为兼容旧路径；CLI 会优先读取根目录的 `interview_qa/`。
