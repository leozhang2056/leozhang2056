# Career KB Project - AI Assistant Rules
# 个人职业知识库项目 - AI 助手规则

---

## 项目定位

这是一个 AI 可读的结构化职业知识库 + 简历生成系统。
核心目标：作为数据层支持 AI 驱动的简历/求职信生成和面试准备。
系统已完整实现：生成器、验证器、CLI 全部可用。

---

## 已确认的关键事实（绝对不得修改或质疑）

| 字段 | 值 |
|------|-----|
| 姓名 | Yuchao Zhang（Leo Zhang） |
| 毕业时间 | **February 2026** |
| 学位 | Master of Computer and Information Sciences @ AUT |
| 荣誉 | **First Class Honours，GPA 8.25/9.0** |
| 工作经验 | **10+ years** |
| 论文 | ChatClothes，DOI `10.1109/IVCNZ67716.2025.11281834` |
| 论文提交 | 提前 6 个月提交（ahead of schedule by 6 months） |
| 目标岗位 | Android / Java 后端 / AI/ML / 全栈（全方向覆盖） |
| 面试语言 | **英文** |

---

## 英文面试稿表达偏好（用户固定）

- **口语化**：短句、好换气、像聊天；可少量自然填充（如 *I mean* / *honestly* / *yeah*）。
- **简单词**：优先常见高频英语；少堆术语——能用 *fast*、*chat app*、*small computer*、*stay up* 就别用 *low latency*、*real-time messaging*、*edge*、*ship* 等（**项目名、技术栈专名可保留**）。
- **适用范围**：`interview_qa/` 下口语稿、**Tell me about yourself**、行为题英文口播等。
- **硬约束不变**：量化与经历仍**只能**来自 `kb/`、`projects/*/facts.yaml`；**禁止**为“好念”而编造指标或项目。

---

## 19 个真实项目列表

| project_id | 简述 |
|---|---|
| chatclothes | AI 服装推荐，IEEE 论文，DOI 已确认 |
| smart-factory | 工业 IoT + AI 预测性维护 |
| enterprise-messaging | 企业即时通讯（Android + Java 后端） |
| broadcast-control | 电视台播出控制系统 |
| visual-gateway | 视觉网关系统（安卓网关/断电监控平台） |
| boobit | 数字货币交易 App（Android 客户端） |
| chinese-herbal-recognition | 中草药识别 AI |
| device-maintenance-prediction | 设备维护预测 |
| exhibition-robot | 展览机器人 |
| forest-patrol-inspection | 森林巡检系统 |
| iot-solutions | IoT 解决方案 |
| live-streaming-system | 直播系统 |
| patent-search-system | 专利检索系统 |
| picture-book-locker | 绘本柜系统 |
| school-attendance | 学校考勤系统 |
| smart-power | 智慧电力系统 |
| visit-system | 访客管理系统 |
| banknote-paper-mill | 造币印纸厂高安全/断网环境现场集成与交付 |
| field-weighing-access-control | 入口地磅称重与道闸/报警联动系统（Delphi/.NET） |

---

## AI 使用约束

### 绝对禁止
1. 编造不存在的项目、公司、职责、技能
2. 编造量化指标或成果（如把"提升了性能"写成"提升了 40%"）
3. 把"熟悉/了解"升级成"负责/主导"
4. 修改已确认的关键事实（毕业时间、GPA、DOI 等）
5. 在没有读取 KB 数据的情况下生成简历内容

### 必须遵守
1. 所有 resume 内容必须从 `kb/` 或 `projects/*/facts.yaml` 生成
2. 修改 `facts.yaml` 后**必须**运行 `python app/backend/validate.py`
3. 新增项目**必须**遵循 `kb/schema/project_facts_schema.yaml`
4. 新增 bullets 必须包含 `id, category, tags, variants, evidence`
5. 如果 KB 中缺少信息，输出 `MISSING_INFO`，提示用户补充后再生成
6. 对话中出现的真实项目情形（问题、取舍、复盘）要**持久化到工程**：先记入 `Interview/behavioral_common_qa.md` 附录/对应题，再在可核对时同步到对应 `projects/*/facts.yaml`

---

## 核心工作流与规范

具体的数据加载顺序、生成命令架构及详细调试指南，请统一参阅 **[AGENTS.md](file:///e:/Coding/leozhang2056/AGENTS.md)**。
修改数据或配置后，请运行：
```bash
python app/backend/validate.py
```
进行格式及完整性验证。
